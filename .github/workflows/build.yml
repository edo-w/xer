name: build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.0
      - uses: ./.github/actions/setup
      - name: run build
        run: pnpm build
      - name: run tests
        run: pnpm test
