# https://lefthook.dev/configuration/

# pre-commit:
#   parallel: true
#   jobs:
#     - name: test
#       run: mise exec -- pnpm test
#       glob: "**/*.{ts,tsx,js,jsx}"
#       exclude:
#         - '*.config.{js,mjs}'

#     - name: lint
#       run: mise exec -- pnpm lint-staged

# commit-msg:
#   commands:
#     commit-lint:
#       run: mise exec -- pnpm commitlint --edit "$1"

